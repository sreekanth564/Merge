pipeline {
    agent any
	stages
      {
        stage ('Checkout scm') {
            steps {checkout scm	}
					}
		stage ('Run UnitTests on Merge') {
                steps {
                     bat ''' git checkout m1
                             git pull origin m1
                             git merge origin f1
                             git push -u origin m1 '''
                       }
                       }
                       }  
		       }

